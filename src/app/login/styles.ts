const styles = {
  container: {
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    gap: '10px',
  },

  button: {
    height: '40px',
    width: '100%',
  },

  gridContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
}

export default styles
